# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets (commit .env.example, never .env)
.env
*.env
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Design-bundle download archives (extracted copies live under design/)
*.zip
# Stray saved-webpage cruft that has wandered into assets/ before
assets/**/*_files/
"assets/**/*Reader.html"

# uv.lock is intentionally NOT ignored — it pins deps for reproducible installs.
